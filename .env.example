# Optional: URL for PDF download (used by website build)
PDF_DOWNLOAD_URL=

# For Vercel MCP API auth, set your API key in the Vercel project environment (not here).
# For local dev, no secrets are required in this repo.
